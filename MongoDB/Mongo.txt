Schema=>
Schema define the shape of the documents within that collection.

const studentSchema = new mongoose.Schema({
    name: String,
    age: Number,
    course: String
});

The permitted SchemaTypes are:

String
Number
Date
Buffer
Boolean
Mixed
ObjectId
Array
Decimal128
Map
UUID
Double
Int32